# Qwen3.5 needs a very recent transformers. If the model fails to load, install
# transformers from source:  pip install "transformers @ git+https://github.com/huggingface/transformers.git"
torch>=2.4
transformers>=4.57
accelerate
peft
torchao>=0.16.0
jupyter
ipykernel
# Optional — only if your model variant requires it (the notebook's cell 0 installs it):
# flash-linear-attention
